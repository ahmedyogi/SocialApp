export type user = {
    id: string,
    email: string,
    username: string,
    imageUrl?: string,
    token: string
}
export type RegisterCreds = {
    username: string,
    email: string,
    password: string
}